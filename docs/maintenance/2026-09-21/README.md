# 快照完整性与恢复登记 — 2026-09-21

## 已复现的问题

旧快照校验器只检查清单中列出的文件；删掉必需文件条目、加入额外文件、将数据文件替换为符号链接或改写版本身份，都可能被接受。

## 修改与验证

校验必需文件、精确目录清单、普通文件及 identity/version 一致性。损坏的未登记快照不能在重试时进入目录表。旧版缺少 input-record-hashes.json 的快照仍可只读校验，但不能作为增量父版本。

58项测试通过。6项单机Ray/驱动退出/重复投递/增量/篡改/孤立快照恢复检查通过；640个查询变体、1920个BM25分数与保存记录一致。完整数据资产重建通过。

[验证摘要](evidence/validation.json)。本次是新的维护记录，原始数据、预测、报告与日期沿革不变。哈希和一致性检查不验证恶意伪造的整条证据链，也不构成生产系统或模型收益证明。
